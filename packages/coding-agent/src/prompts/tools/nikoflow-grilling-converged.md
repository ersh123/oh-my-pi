Record that Nikoflow grilling has converged.

Call this only when every material ambiguity has been resolved or converted into an explicit assumption. If any material question remains open, include it in `open_questions`; the call will be rejected and the grilling gate will stay closed.
