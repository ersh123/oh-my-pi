Define the Nikoflow ticket DAG for the current Ticketization phase.

Call this exactly once during Ticketization before asking for human approval. Submit vertical implementation tickets in dependency order. Each ticket needs a stable id, acceptance criteria, blocking ticket ids, and implementation notes.

This tool is the only supported way to capture Nikoflow tickets into executable state.
